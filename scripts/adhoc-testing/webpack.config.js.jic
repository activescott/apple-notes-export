/* eslint-env node */
const path = require("path")

module.exports = {
  entry: "./adhoc-testing.ts",
  output: {
    filename: "adhoc-testing.bundle.js",
    path: path.resolve(__dirname)
  },
  mode: "development",
  devtool: "(none)",
  resolve: {
    extensions: [".ts", ".js"]
  },
  module: {
    rules: [
      { test: /\.ts$/,
        loader: "babel-loader",
        options: {
          presets: [
            [
              // see https://babeljs.io/docs/en/babel-preset-typescript
              "@babel/preset-typescript",
              {
                // see https://babeljs.io/docs/en/babel-plugin-transform-typescript#typescript-compiler-options
              }
            ],
            [
              "@babel/preset-env",
              {
                targets: {
                  safari: "13"
                },
                useBuiltIns: "usage",
                corejs: 3
              }
            ]
          ]
        }
      },
      {
        test: /\.m?js$/,
        exclude: /(node_modules|bower_components)/,
        use: {
          loader: "babel-loader",
          options: {
            presets: [
              [
                "@babel/preset-env",
                {
                  targets: {
                    safari: "13"
                  },
                  useBuiltIns: "usage",
                  corejs: 3
                }
              ]
            ]
          }
        }
      }
    ]
  }
}
